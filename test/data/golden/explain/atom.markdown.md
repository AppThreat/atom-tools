# Flow explanation — atom (reachables mode; no version recorded)

> Generated by `atom-tools explain`; deterministic template rendering over the unified flow model — no model call. Every clause maps to a field; hedged clauses say what the report does not know.

## Summary
- severity: 0 error / 54 warning / 109 note; 0 engine-reported, 163 derived
- source tags: 78 of 163 flow(s) carry tags on the source node
- entry-point join: 2 of 163 flow(s) sit behind an anchored entry point; atom: 1 entry point(s) from route tags or usages conversion (atom emits no call graph).

## Flows (163 matching, ranked by severity then entry-point tier)

### 1. `atom-flow-160` — warning (derived)
- **Reachability:** `pkg:maven/org.springframework/spring-web@7.0.8?type=jar` is reached from `showOwner` (src/main/java/org/springframework/samples/petclinic/owner/OwnerController.java); atom does not classify authentication, so the route's exposure is unknown.
- **Path:** source `ownerId` (src/main/java/org/springframework/samples/petclinic/owner/OwnerController.java:170) tagged `framework-input` which reaches `return mav;` (src/main/java/org/springframework/samples/petclinic/owner/OwnerController.java:176) in 12 step(s), with no sink category recorded in this slice.
- **Evidence:** atom (no version recorded in the slice), reachables mode. Severity `warning` (derived from the shared taxonomy, not engine-reported).

### 2. `atom-flow-74` — warning (derived)
- **Reachability:** `pkg:maven/org.springframework/spring-web@7.0.8?type=jar` is reached from `showOwner` (src/main/java/org/springframework/samples/petclinic/owner/OwnerController.java); atom does not classify authentication, so the route's exposure is unknown.
- **Path:** source `ownerId` (src/main/java/org/springframework/samples/petclinic/owner/OwnerController.java:170) tagged `framework-input` which reaches `addObject` (src/main/java/org/springframework/samples/petclinic/owner/OwnerController.java:175) in 12 step(s), with no sink category recorded in this slice.
- **Evidence:** atom (no version recorded in the slice), reachables mode. Severity `warning` (derived from the shared taxonomy, not engine-reported).

### 3. `atom-flow-101` — warning (derived)
- **Reachability:** `pkg:maven/org.springframework.data/spring-data-commons@4.1.0?type=jar` (and 1 more package(s)) is reached by this flow; no route-tagged node (framework-route/service-ingress) sits on its path.
- **Path:** source `page` (src/main/java/org/springframework/samples/petclinic/vet/VetController.java:45) tagged `framework-input` which reaches `addAttribute` (src/main/java/org/springframework/samples/petclinic/vet/VetController.java:55) in 14 step(s), with no sink category recorded in this slice.
- **Evidence:** atom (no version recorded in the slice), reachables mode. Severity `warning` (derived from the shared taxonomy, not engine-reported).

truncated: 160 more flow(s) not shown — the listing is ranked by severity then entry-point tier; select with --flow/--package/--file/--query or raise --max-flows.
